import matplotlib.pyplot as plt
import numpy as np

# # a = np.random.random((16, 16))
# a = [[np.random.randint(0, 2) for _ in range(16)] for _ in range(16)]

# mat = np.matrix(a)
# with open('./test_heatmap.txt','wb') as f:
#     for line in mat:
#         np.savetxt(f, line, fmt='%.2f')

# plt.imshow(a, cmap='hot', interpolation=None)
# plt.savefig('./test_heatmap.pdf', format='pdf', dpi=1200, bbox_inches='tight')


